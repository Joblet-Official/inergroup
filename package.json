{
  "name": "inergroup-job-search",
  "version": "1.0.0",
  "private": true,
  "description": "MCP integration for searching current Inergroup Insourcing Solutions job listings supplied through Joveo.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "tunnel": "npx cloudflared tunnel --url http://localhost:3001",
    "test": "node --import tsx --test --test-concurrency=1 server/test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "keywords": [],
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "saxes": "6.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
